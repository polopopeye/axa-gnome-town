// export const handleSetQuery = () => {
//   props.setQuery(document.getElementById('query').value);
// };
// export const handleSetLimits = (setLimitPerPage) => {
//   actualPage = 1;
//   props.setLimits(setLimitPerPage);
// };

// export const handleSetFavourite = (id) => {
//   props.setFavourite({
//     id: Brastlewark[id].id,
//     name: Brastlewark[id].name,
//   });
// };

// export const handleDeleteFavourite = (id) => {
//   props.deleteFavourite(id);
// };
